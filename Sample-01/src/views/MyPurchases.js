import React, { useEffect, useState } from 'react';
//import { getPurchases } from '../api/flights';
import { Link } from 'react-router-dom';

function PurchaseList() {
    const [purchases, setList] = useState([]);

    return (
        <h1>My Flights</h1>
    )
}

export default PurchaseList;